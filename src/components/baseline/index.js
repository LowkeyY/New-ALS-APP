
import './index.less';

const PrefixCls = 'baseline';
const BaseLine = () => (
  <div className={`${PrefixCls}-baseline`}>
    <div className={`${PrefixCls}-baseline-infos`}>
      <span>没有更多内容了</span>
    </div>
  </div>
);
export default BaseLine;
